# 小程序与 H5 部署

本目录包含用户端源码，不单独拥有数据库。三端共用后端数据库，初始化包在后端仓库 deploy/database/；整体步骤见 https://github.com/sickelgroupe-create/sjz-club-backend/tree/main/deploy 。

本仓库已独立化，不要求若依目录相邻。需要同步后端页面路由时，显式设置 CLUB_BACKEND_ROOT 为后端仓库路径，再运行 npm run sync:routes。
不要改变现有界面。修改业务代码前阅读 AGENTS.md 和 UI_FREEZE.md，并运行 npm run check:ui-freeze。

## 新电脑构建

安装 Node.js、npm、Windows PowerShell、微信开发者工具。运行 npm ci。
核对 .env.production 和 .env.development，把 VITE_API_BASE_URL 改成新服务器自己的 API 地址，例如 https://api.example.com/app。
本地已有环境文件可能指向现网；构建前必须替换，不能直接沿用线上地址进行测试。
AppID 配置在 manifest.json / project.config.json，换主体时需按实际小程序修改；不要放 AppSecret 或支付密钥。

运行 npm run build:mp-weixin，检查生成的 dist 目录，用微信开发者工具导入该目录，确认项目 AppID、合法域名及服务类目。
如需 H5，运行 npm run build:h5，将 dist-h5 内容部署到 H5 站点目录。构建脚本使用 Windows PowerShell，在 Linux 上需提供兼容 PowerShell 或在 Windows 构建后上传产物。

## 必须单独配置

微信后台的服务器域名、HTTPS、登录/手机号能力、支付/虚拟支付、回调和商品映射不会自动随源码迁移。
从空库部署时商品与实际用户为空，需要在管理后台录入。迁移现网还需安全交付实际数据库备份、上传素材和服务器私密配置。
本次目录整理没有执行微信上传、支付、退款或线上部署。
